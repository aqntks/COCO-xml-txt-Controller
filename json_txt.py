import json
import os
import sys

with open('json', 'r')as j:
    json_data = json.load(j)

    json_data['images'].
    json_data['images']['file_name']

    json_data['images']['image_id']

    json_data['annotations']['image_id']

    json_data['annotations']['text']

f = open("det.txt",'w')
for i in range(0, 20):
    data =
    f.write(data)
f.close()